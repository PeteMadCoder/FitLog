# Tasks

## Fitlog app

Dark mode.

Localize strings (i18n) and allow users to choose language in the settings tab.

Auto-pause logic (still need to look into this).


## CI/CD

Make GitHub actions:
 - Run tests (flutter and in future python). Probably bash script
 - Compile the app (flutter build apk) and make the apk as a package (check this better in the future)

