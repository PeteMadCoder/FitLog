# Tasks


In the workout summary page, have a button to export the workout (that single workout) as GPX.

Allow the user to define weekly "goals" (in the settings tab). Then in the main page, the user can see for how many weeks they've managed to accomplish that goal. Also, they can see their progress for that week's goal.

Dark mode.

Localize strings (i18n) and allow users to choose language in the settings tab.

Auto-pause logic (still need to look into this).
