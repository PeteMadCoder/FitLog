# Tasks


In the workout summary page, have a button to export the workout (that single workout)

Allow the user to define "goals" (in the settings tab) for that week. Then in the main page, the user can see for how many weeks they've managed to accomplish that goal. Also, they can see their progress for that week's goal.

Auto-pause logic (still need to look into this).

Dark mode.

Localize strings (i18n) and allow users to choose language in the settings tab.
